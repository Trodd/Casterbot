"""CasterBot — Discord bot for match caster/cam-op claim system."""

# === MAINTAINABILITY / AGENTS AUDIT ANNOTATIONS ===
# Code smell: package root does not define an explicit typed public API surface (__all__).
# Code smell: no module-level version metadata, which can hinder deployment introspection.
# AUDIT COUNTS: format gate passed for this file; ruff findings=0; pyright findings=0.
# AUDIT SCOPE: package initialization is intentionally tiny, but repo layout still violates AGENTS src-layout guidance.
